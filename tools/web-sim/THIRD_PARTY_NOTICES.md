# Third-party notices

The generated Creator Club browser preview bundles
[three.js](https://threejs.org/) 0.185.1, which is distributed under the MIT
License. The bundle is built with legal comments preserved.
The full notice is tracked in `vendor/three-LICENSE.txt` and injected into each
generated standalone study before the bundled renderer.

The cascade and Creator Club rendering code in this directory is original
project code. The older Juggler3D/JuggleSaver implementations informed the
project's architectural research but were not copied into this implementation.
